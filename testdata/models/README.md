# Test models

`mock-*.json` are "model files" for the mock engine (`internal/engine/mock`):
each is a `mock.Config` overlay. Real GGUF files are downloaded here by
`make testdata` and are git-ignored.
