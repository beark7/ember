// Package auth handles API keys (emb_ prefix, Argon2id hashes, scopes, expiry),
// the admin session with CSRF protection, roles and the localhost policy.
// See docs-private/design/server.md and ADR-008.
package auth
