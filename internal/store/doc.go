// Package store owns the SQLite database (pure Go driver), embedded forward-only
// migrations and the repository interfaces used by every other package.
// See docs-private/design/store-config.md and ADR-005.
package store
