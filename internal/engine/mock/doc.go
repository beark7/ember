// Package mock is a deterministic in-process engine used by server, scheduler,
// accounting, console and contract tests: configurable speed, canned replies,
// scripted tool calls and failures. See docs-private/design/engine.md.
package mock
