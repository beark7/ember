# Copy to .env for local development. .env is git-ignored; never commit secrets.
# Every key in config.yaml can be overridden as EMBER_<SECTION>_<KEY>.
EMBER_HOME=
EMBER_SERVER_LISTEN=127.0.0.1:11500
EMBER_API_KEY=
